{# templates/comment/newc.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create new Comment{% endblock %}

{% block body %}
    <div class="container mt-4">


        {{ form_start(form, {'attr': {'class': 'form'}}) }}

        <div class="mb-3">
            {{ form_row(form.Content, {'attr': {'class': 'form-control'}}) }}
        </div>

        <button type="submit" class="btn btn-success">{{ button_label|default('Save') }}</button>

        {{ form_end(form) }}

        <a href="{{ path('app_annonce_show', {'id': annonce.id}) }}" class="btn btn-primary mt-2">Back to Annonce</a>
    </div>
{% endblock %}
